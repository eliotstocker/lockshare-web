{
  "name": "locksha.re",
  "version": "1.0.0",
  "description": "Website for LockSha.re",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "sergey --watch --exclude=.idea --content=content",
    "build": "sergey --exclude=.idea --content=content"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "sergey": "0.0.13"
  }
}
